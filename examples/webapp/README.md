# Legacy Flask Demo

This web app is provided as a non-core example.

For production usage, prefer:

- Python API (`lunadem.generate_dem`, `lunadem.analyze_dem`, `lunadem.assess_landing`)
- CLI (`lunadem generate`, `lunadem analyze`, `lunadem landing`)
